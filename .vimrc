" === Custom VIM Configuration === 

" 1. Enable line numbers for easier navigation
set number 

" 2. Highlight search results so matches stand out
set hlsearch 

" 3. Enable auto‑indent to keep code nicely formatted
set autoindent

" 4. Highlight current line
set cursorline

" 5. Enable mouse support 
set mouse=a
